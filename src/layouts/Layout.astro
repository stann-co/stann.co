---
import "../styles/global.css";

interface Props {
	title?: string;
}

const { title = "STANN.co" } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<!-- video.twimg.com 403s any request with a Referer header (hotlink
		     protection), so suppress the referrer page-wide. The image host and
		     Google Fonts both load fine without one. -->
		<meta name="referrer" content="no-referrer" />
		<title>{title}</title>

		<!-- Google Fonts: Atkinson Hyperlegible Next -->
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			href="https://fonts.googleapis.com/css2?family=Atkinson+Hyperlegible+Next:ital,wght@0,200..800;1,200..800&display=swap"
			rel="stylesheet"
		/>
	</head>
	<body>
		<slot />
	</body>
</html>
